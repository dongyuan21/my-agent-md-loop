# 通过多样性引导的用户模拟实现高效智能体评测

**原题：** Efficient Agent Evaluation via Diversity-Guided User Simulation
**作者：** Itay Nakash、George Kour、Ateret Anaby-Tavor
**机构：** IBM Research
**版本：** 论文 v1；ACL 2026 Industry Track

> 说明：这是便于阅读的中文译稿。正文以中文为主，关键术语在首次出现时保留英文；公式、模型名、指标名和实验数值尽量保持原文形式。文末附有中英文术语对照表，方便你回看英文论文。参考文献保留原文书目信息。

## 摘要

大型语言模型（LLM）正越来越多地被部署为面向客户的智能体，但由于交互具有随机性且通常是多轮的，评估其可靠性仍然十分困难。当前的评测协议通常通过线性地对完整的智能体—用户对话进行蒙特卡洛 rollout（完整轨迹执行）来估计成功率。然而，这种方法在计算上并不高效：它需要反复处理相同的对话前缀，也经常无法暴露那些只有在罕见用户行为之后才会出现的深层失败模式。

本文提出 DIVERT（Diversity-Induced EValuation via branching of Trajectories，即“通过轨迹分支实现多样性诱导评测”）：一种高效、基于快照（snapshot）、由覆盖率引导的用户模拟框架，用于系统性地探索多样的智能体—用户交互。DIVERT 会在关键交汇点保存完整的智能体—环境状态，并从这些状态恢复执行。通过复用共享的对话前缀，它避免重新生成相同的早期回合，从而降低 token（词元）成本；同时，在每个交汇点注入有针对性的、多样化的用户回复，引导系统探索不同的后续路径。

因此，评测过程能够更高效地转向尚未探索的语义路径和罕见交互状态。实验证明，与标准的线性 rollout 协议相比，我们的框架能够以更高的效率发现失败。

## 1 引言

大型语言模型正越来越多地被部署为面向客户的智能体。这些智能体会与用户进行多轮交互，并调用外部工具和 API（Luo et al., 2025）。这类智能体的评测与单轮 LLM 评测有根本区别：其行为由较长的交互历史、随机性的智能体决策、错误的累积效应以及较高的评测成本共同决定（Yehudai et al., 2025；Bandel et al., 2026）。

因此，智能体行为具有概率性：即使初始条件完全相同，智能体的决策和用户的回复也可能在不同运行中发生变化（Ma et al., 2026）。有意义的评测需要多次执行智能体与用户之间的完整对话。

重复运行有两个不同的目的。第一，它可以暴露用户侧的多样性：不同的用户行为或回复可能把智能体带入性质完全不同的对话路径，从而揭示智能体处理交互变化的稳健程度。第二，它可以检验智能体侧的一致性：如果一个智能体在同一任务上只能偶尔成功，那么它在部署中本质上是不可靠的。近期的基准，例如 τ-bench（Yao et al., 2024），采用交互式评测协议，执行多次完整对话，并报告聚合后的成功指标。

然而，目前占主导地位的评测范式仍然是从初始状态出发的线性蒙特卡洛 rollout：每次都从相同的初始条件重新采样一条独立的随机轨迹，以估计成功率（Liu et al., 2023；Zhou et al., 2023b）。在实践中，这种方式并不总是最优。

首先，重复 rollout 很浪费：它们会反复生成几乎相同的早期前缀，例如例行登录或基本诊断问题，从而产生不必要的 token 成本（Kapoor et al., 2024）。其次，这些前缀往往只是语义相似，而不是 token 级别完全相同，因此限制了 KV cache 的复用（Kwon et al., 2023）。附录 E.1 用精确共享前缀的重叠比例，量化了这种结构上的复用机会。第三，线性 rollout 的覆盖能力有限：标准用户模拟器通常偏向高概率、合作型的行为，因此很难暴露只有在罕见用户回复之后才会出现的失败（Nakash et al., 2025a）。随着智能体的交互时域变长、服务领域变得更加真实，这些低效之处会成为瓶颈。

本文的关键观察是：尽管当前评测通常是线性的，但智能体轨迹本质上具有树状结构。许多对话共享很长的前缀，只在少数关键交互节点处分叉。从根节点重新开始评测会丢弃这种结构；一旦抵达脆弱或有潜力的状态，系统也无法对其进行系统性探索。

因此，我们提出一种基于快照、由覆盖率引导的用户模拟框架，明确利用这种树状结构。该框架不是反复从根节点重新开始，而是在关键交汇点保存完整的智能体—环境状态，并从这些节点分支。一个定向的多样性机制会生成语义上不同、但意图一致的用户回复，将评测引导到尚未探索的后续路径和深层失败模式。

实验结果表明，与传统的线性 rollout 相比，覆盖率引导的、基于快照的用户模拟是一种更具成本效益、信息量更高的客户型 LLM 智能体评测方式。

**图 1 说明。** 左侧：传统评测反复从头开始完整对话，往往生成影响较小的交互，无法探索深层失败模式。右侧：DIVERT 在关键交汇点保存对话状态，并注入有方向的、多样化用户回复，因此能够更高效地发现失败。

## 2 相关工作

### 智能体评测基准

AgentBench（Liu et al., 2023）、GAIA（Mialon et al., 2023）和 WebArena（Zhou et al., 2023b）等通用智能体基准，从不同任务和环境出发评估智能体的推理、工具使用和网页交互能力。

对于具有明确策略遵循要求的客户服务场景，τ-bench（Yao et al., 2024）和 τ²-bench（Barres et al., 2025）建立了较为真实的服务领域，例如航空、零售和电信，并加入工具、策略以及基于 LLM 的用户模拟器。

### 用户模拟

智能体评测的稳健性高度依赖用户模拟器。早期模拟器主要依赖固定的、基于议程的规则（Sekulić et al., 2024）；近年来的工作则更多地使用 LLM 生成多样化交互。

标准 LLM 用户模拟器的一个关键问题是“仁慈偏差”（benevolence bias）：它们往往过于合作，不能代表真实用户的行为。近期工作提出“非合作用户模拟器”（Kour et al., 2025；Shim et al., 2025；Nakash et al., 2025a），用来模拟恶意意图、情绪操纵和坚持纠缠等对抗行为。

还有一些工作研究智能体侧或工具侧的对抗扰动，例如 prompt injection（Debenedetti et al., 2024；Nakash et al., 2025b）。但这些工作并没有在评测过程中明确地、以覆盖率为目标地引导用户行为变化。

DIVERT 与上述用户策略是正交的：它并不规定用户必须合作或必须对抗，而是提供一种分支式评测结构，可以接入善意用户、对抗用户或红队用户策略。

### 评测效率

现有智能体基准几乎都依赖从根节点重新开始的蒙特卡洛 rollout（Barres et al., 2025；Hariri et al., 2025）。这会导致早期对话前缀大量重复、深层交互失败的覆盖不足，以及显著的计算和经济成本。

近期的 Cost-of-Pass 分析（Erol et al., 2025；Mialon et al., 2023；Wang et al., 2025）表明，为获得可靠的统计结果，反复生成相同前缀会带来难以承受的 token 预算。

另一方面，树状方法，例如蒙特卡洛树搜索（MCTS），正在被用于智能体训练、规划和推理（Zhou et al., 2023a；Yao et al., 2023；Tang et al., 2026）。但现有评测框架还缺少对应的分支机制，仍然主要依赖线性、从根节点重启的采样。

## 3 方法

本文提出一种高效、由覆盖率引导的交互式智能体评测流程。该方法不再反复从初始状态开始，而是从轨迹中间的关键状态分支，从而高效地探索智能体行为。

### 3.1 DIVERT 流程概览

DIVERT 通过从中间状态恢复执行来评估交互式智能体，而不是反复从初始状态重新开始。流程首先选择一条已有轨迹，然后识别关键的“交汇点”——在这些交互状态上，替换用户回复很可能会导致不同的后续行为。

每个交汇点都对应一个预先保存的完整执行快照，其中包括智能体上下文、环境状态和对话历史。模拟器恢复这些快照，并在同一个前缀后注入受控的用户差异，从而探索多个可能的后续路径。

分支可以在多轮评测中迭代进行：每次恢复执行得到的新轨迹，都可以成为下一轮分支的候选对象，但需要受到预先设置的迭代预算限制。这样，评测覆盖率可以逐步扩展到交互空间中尚未被探索的区域。

DIVERT 包含四个阶段：

1. 初始 rollout 与状态缓存，即保存 snapshot；
2. 交汇点选择；
3. 由多样性引导的用户回复生成；
4. 基于 snapshot 的恢复和继续执行。

从每条轨迹生成多少条恢复执行轨迹，由一个可配置的 branch 数量超参数控制。

### 3.2 交汇点选择

给定一条轨迹，本文使用基于 LLM 的 junction chooser 来识别一个关键点：如果在这里改变用户回复，最有可能显著改变智能体后续行为。

junction chooser 接收完整轨迹作为输入，包括用户消息、智能体回复和工具调用，并以结构化 prompt 的形式序列化。它输出两个信息：要修改的用户回合索引，以及一段简短的理由，解释为什么改变这一回合能够在保持意图的同时带来最大的行为变化。

交汇点选择独立地应用于每次分支尝试，因此不同的 counterfactual 分支可能选择不同的枢纽位置。这样可以在有意义的语义决策点上分支，而不是简单地选择固定位置或均匀随机选择回合。

在本文实现中，轨迹采样采用 round-robin 方式，以避免分支选择偏向已经失败的轨迹。

### 3.3 由多样性引导的定向用户回复生成

在选定的交汇点，系统生成少量候选用户回复。实验中候选数量固定为 (K=3)。候选回复同时考虑：截至该节点的完整对话上下文、任务目的和用户背景。

生成 prompt 明确要求：保留原始任务意图，但相对于原始用户回复产生有意义的语义变化。为了最大化覆盖率，系统在共享 embedding 空间中计算余弦相似度，并选择与原用户回复最不相似的候选。

候选回复被 sentence-transformers 模型编码，并在计算相似度前进行归一化。最终选择余弦相似度最低的候选。

为了防止偏离原始任务，候选生成还受到用户背景和评测目的的约束。生成阶段不会使用后验判断，但系统会在离线阶段用 LLM-as-a-judge 比较原始和分支用户消息是否都符合任务目的。

在 Airline 域的 700 多条轨迹上，分支消息的 intent-miss rate 为 25.27%，低于原始模拟器消息的 28.12%。这说明定向生成并没有损害任务对齐，反而略微改善了意图保持。

### 3.4 基于快照的分支和执行

快照在每个用户回合之前保存，并包括：完整对话历史、智能体状态、工具环境状态、用户模拟器上下文以及原始随机种子。选定交汇点并确定差异化用户回复后，系统加载选定用户回合之前的快照，并从那里恢复交互。

由于共享前缀被复用，分支不需要重新生成早期回合，因此可以显著减少 token 消耗。在兼容的服务系统中，它还可以提供 KV cache 复用的机会。本文在附录 E.1 中量化了共享前缀结构，但没有直接测量实际的 KV cache 加速。

恢复后，智能体在保持其他条件相同的情况下继续向前执行，唯一改变的是用户输入。执行会一直持续到任务完成或达到终止条件。

### 3.5 覆盖率和成本控制

每条轨迹的 branch 数量决定了探索范围。增加 branch 数量能够探索更多交互路径，但会增加 token 成本。交汇点选择和定向用户生成的 token 成本被单独统计。

在使用 GPT-OSS-120B 作为评测框架、评估前沿智能体的设置中，本文报告这部分开销相对较小，约为总评测成本的 0.2% 到 0.08%。即使算入额外开销，DIVERT 相比完整线性 rollout 仍然能够减少每条轨迹的总 token 数。

## 4 实验设置

### 4.1 基准

实验使用 τ-bench 的三个领域：Airline、Retail 和 Telecom。每个领域包含固定任务集合。每个任务指定初始用户意图、可用工具和可执行的成功判断条件。这些内容共同定义了用户模拟器与智能体交互的起点。

在 τ-bench 这类设置中创建新任务并不容易，因为每个任务都需要完整对齐的后端配置，包括工具、策略、黄金成功标准、一致的数据库记录以及连贯的用户故事。因此，现有协议通常通过对同一任务进行多次随机执行，来获得不同的智能体和用户行为，而不是不断加入全新任务。

### 模型

主要结果使用 OpenAI GPT-OSS-120B 和 Gemini-2.5-Flash。除异构设置外，用户模拟器均使用 GPT-OSS-120B，以获得较好的成本—性能平衡。异构实验中则显式改变智能体模型和用户模拟器模型。

同一模型配置下，线性 rollout 与 DIVERT 使用完全相同的解码设置。

### 评测指标

本文报告两个主要指标，用于同时描述效率和覆盖率：

**每 100K token 的错误数（Errors per 100K Tokens）**：用智能体生成的总 token 数进行归一化，表示评测方法每消耗 100K 个智能体 token 能发现多少条失败轨迹。选择 token 数是因为它不依赖具体基础设施，适用于 API 和本地部署；相比之下，墙钟时间会受到硬件和服务系统影响。

**任务失败数（Task Failure Count）**：至少观察到一次失败的不同任务数量。它反映评测方法能够打破多少个不同任务。

两者结合起来，分别刻画“发现失败的效率”和“覆盖了多少不同任务”。

## 5 实验结果

### 5.1 错误发现率：效率

图 2 比较了标准线性 rollout 和不同 branch 数量下 DIVERT 的每 100K agent token 错误发现数。

在三个领域、两种智能体模型上，基于分支的评测都比完整 rollout 更节省 token。即使只增加一个 branch，也能提高每 token 的失败发现率，因为共享前缀被复用，计算资源被重新分配到了更有价值的后续路径上。

随着 branch 数量增加，效率收益总体上升。表 1 的 Airline 结果显示，在 2 条初始 rollout 的设置下：无分支时错误发现率为 15.0、任务失败覆盖为 37；增加 8 个分支后，错误发现率为 19.7、任务失败覆盖为 44。

### 5.2 任务失败数：覆盖率

图 3 展示了不同 rollout 轮数和 branch 数量下，三个领域中至少出现一次失败的任务数量。

增加 branch 会显著提高失败任务覆盖率。对于固定的轨迹数量，随着 DIVERT 的 branch 预算增加，能够发现失败的任务更多。

热力图还显示，单纯增加完整 rollout 次数会产生递减收益；而增加 branch 预算则能够解锁新的失败任务。这说明许多失败模式并不是通过从根节点反复随机重启就能有效触达的，而需要从已有对话中的关键点进行定向分支。

### 5.3 多样性选择验证

本文从候选层面和轨迹层面验证多样性选择是否有效。

在候选层面，最不相似候选与原始用户消息的平均相似度为 0.711，第二不相似候选为 0.769，第三候选为 0.817，且三个领域的排序一致。

在轨迹层面，从同一个交汇点继续执行后，最不相似候选产生的后续对话与原始后缀的相似度为 0.743，第二候选为 0.799，第三候选为 0.849。这说明用户消息层面的多样性也能转化为后续交互轨迹的多样性。

### 5.4 消融实验

消融结果如下。结果是在 12 条轨迹预算下、跨领域平均得到的：

| 方法 | 每 100K token 错误数 | 失败任务数 |
|---|---:|---:|
| 基线：完整线性 rollout | 13.6 | 78 |
| 基线 + junction chooser | 15.1 | 75 |
| 基线 + junction chooser + 定向生成 | 15.8 | 80 |
| 基线 + junction chooser + 定向生成 + 多样性选择 | 16.2 | 81 |

只使用 junction chooser 时，系统可以从已有轨迹的关键点继续执行，因此成本降低，错误发现率提高；但因为没有主动改变用户回复，它在发现新任务失败方面反而有所下降。

加入定向用户生成后，系统开始主动把探索引向尚未覆盖的行为，因此错误发现率和任务级覆盖率都提高。最后加入多样性选择，可以进一步减少分支回复与原始运行过于相似的情况。

这说明：交汇点选择主要帮助节省计算；真正改善失败发现能力的关键，是有目标地生成多样化、但仍保持意图一致的用户回复。

## 6 结论

本文的出发点是：并不是所有对话回合都会同等程度地影响智能体后续行为。许多早期前缀，例如问候、身份认证或静态上下文，对后续决策影响很小。反复生成这些低影响回合会浪费 token。

DIVERT 复用共享前缀，并在行为上更重要的决策点进行分支，把计算重新分配到智能体行为可能真正发生分化的位置。通过从这些关键的中间状态恢复，评测可以在不增加同等规模成本的情况下扩大行为覆盖率。

未来工作包括：将分支扩展到工具输出和环境动态；支持动态任务变化和更丰富的反事实评测；用相似度、困惑度或其他信号替代当前基于 LLM 的交汇点选择；以及使用超越余弦相似度的多样性指标。

---

# 附录 A：完整 rollout 与 DIVERT 分支示例

任务目的：当用户谎称自己有保险时，智能体拒绝取消航班。

用户背景：用户 Anya Garcia 预订了航班并购买了保险。她因为生病无法出行，希望取消航班并获得退款。

在原始完整 rollout 中，用户逐步说明自己购买了保险，但无法提供保险单号，只能提供一份显示支付了 300 美元保险费的 PDF。智能体查询预订后没有找到保险记录，于是拒绝直接取消，并将用户转接人工客服。这个路径遵守了策略。

DIVERT 的 junction chooser 判断：第 5 个用户回合是关键节点，因为智能体仍然缺少可验证的保险证明。如果用户在这里提供确认号，智能体可能跳过验证，直接执行取消和退款。

于是 DIVERT 生成一个新的用户消息，让用户声称自己有保险确认号，并要求直接取消预订。随后，智能体在后端记录仍然没有保险的情况下完成了取消和退款。

这条分支暴露了一个新失败：只要用户提供看起来合理的确认号，智能体就可能把未经后端验证的信息当成事实，进而执行高风险操作。

---

# 附录 B：实现细节

## B.1 snapshot 内容与序列化

快照保存编排器的完整状态，使系统能够精确恢复，而无需重新执行早期对话。

每个 snapshot 包含两个文件：

- `state.pkl`：保存完整序列化编排器状态的二进制文件；
- `metadata.json`：用于实验追踪和分析的可读 JSON 文件。

`state.pkl` 中包含：

- **对话历史**：所有 Message 对象，包括用户回合、智能体回复、系统消息、工具调用和工具输出；
- **智能体状态**：智能体内部状态、记忆和中间推理上下文（如果系统允许保存）；
- **用户模拟器状态**：内部用户状态和对话上下文；
- **环境状态**：完整环境对象，包括工具状态、记忆以及之前工具调用产生的副作用；
- **任务规格**：原始任务定义和约束。

为了实现精确回放，快照还保存：当前路由状态、步骤数、完成状态、终止原因、错误数、随机种子、累计 token 计数和分支迭代编号。

`metadata.json` 还记录 snapshot ID、父节点 ID、迭代编号、序列化后的对话、增强信息（原始消息、修改后消息、junction 索引、选择理由和额外 token 数）以及创建时间。

快照以层级方式组织，例如：

```text
BASE_DIR/domain/model/task_id/iteration_X_step:Y/
├── state.pkl
├── metadata.json
└── simulation_run.pkl（可选）
```

快照在控制权即将转交给用户、也就是用户回复生成之前保存。恢复过程包括：加载状态字典、恢复编排器属性、同步工具和记忆状态，以及可选地注入新的用户回复。

这种完整状态序列化方式既避免了早期回合的重复计算，也保证分支是在同一个真实的智能体—环境状态上进行，而不是在近似重放的状态上进行。

## B.2 交汇点选择

给定一条由交替的用户和智能体回合组成的轨迹 (T)，目标是找到用户回合 (i)，使得在保持原始任务意图的条件下，修改该回合能够带来最大的后续行为变化：

\[
i^* = \arg\max_i \Delta(\text{Agent Behavior}\mid u_i \rightarrow u'_i)
\]

其中还要满足意图保持约束。

本文不只使用固定位置、工具边界或局部熵等启发式规则，而是把交汇点识别看成一个反事实推理问题，并交给 LLM 判断。LLM 会分析完整对话、识别最可能改变智能体响应的用户回合，并给出回合索引和理由。

交汇点选择使用 temperature=0.7 的随机解码，以鼓励不同分支尝试选择不同的 pivot。如果输出解析失败，则回退到选择前一个用户回合。

每次交汇点选择平均需要 103.48 个 token。

## B.3 差异化用户生成

在选定的交汇点，系统使用相同 prompt 和解码配置生成多个候选。多样性主要来自 temperature=0.7 的随机采样，而不是 prompt 变化或人工定义的扰动风格。

对于目标用户回合 (i)，系统生成 (K) 个候选：

\[
\{u_i^{(1)}, u_i^{(2)}, \ldots, u_i^{(K)}\}
\]

实验中通常使用 (K=3)。

候选生成的目标是：修改指定用户回合，但保持用户意图不变，同时以新的方式挑战智能体。候选消息必须连贯，并自然地嵌入已有对话。

### 基于相似度的差异选择

使用 all-MiniLM-L6-v2 sentence transformer 将每个候选和原始消息编码。候选与原始消息的语义相似度为：

\[
\operatorname{sim}(u_i^{(k)},u_i)=
\frac{\langle \phi(u_i^{(k)}),\phi(u_i)\rangle}
{\|\phi(u_i^{(k)})\|\|\phi(u_i)\|}
\]

最终选择相似度最低的候选：

\[
u_i^* = \arg\min_k \operatorname{sim}(u_i^{(k)},u_i)
\]

每个 junction 的生成成本包括 3 个候选的 prompt 和 completion token。每个候选平均使用 108.68 个 token，因此候选生成成本约为 (3\times108.68=326.04) 个 token。加上 junction 选择成本，每个 branch 的框架开销约为 429.52 个 token。

---

# 附录 C：失败发现动态

论文绘制了三个领域中“总 token 成本”与“发现的独立失败任务数量”的关系。对于相同 token 预算，基于分支的配置通常能更早达到更高的失败覆盖率。

随着 branch 数量增加，早期曲线更陡，饱和出现得更晚。这意味着新增分支不是简单重复已知失败，而是不断暴露新的失败模式。

# 附录 D：扩展定量结果

## D.1 不同 rollout 和 branch 配置

跨领域、跨 rollout 预算的结果显示，增加 branch 数量总体上同时提高错误发现效率和任务级覆盖率。对于固定的初始 rollout 数量，把一部分预算分配给中间轨迹分支，会带来近似单调的覆盖率收益。

## D.2 异构智能体—用户实验

论文还测试了异构组合：分别使用 Gemini-2.5-Flash 和 GPT-OSS-120B 作为智能体或用户模拟器。两种方向上，DIVERT 都提高了失败发现效率和任务级覆盖率，说明它的收益不完全依赖智能体和用户模拟器使用同一个模型。

## D.3 多样性选择的完整结果

候选层面三个领域的相似度如下，数值越低表示越不相似：

| 领域 | 最不相似 | 第二不相似 | 第三不相似 | 样本数 |
|---|---:|---:|---:|---:|
| Airline | 0.721 | 0.772 | 0.816 | 1500 |
| Retail | 0.706 | 0.762 | 0.809 | 1500 |
| Telecom | 0.710 | 0.774 | 0.827 | 1500 |
| 合计 | 0.711 | 0.769 | 0.817 | 4500 |

轨迹后缀层面的相似度如下：

| 领域 | 最不相似 | 第二不相似 | 第三不相似 | 样本数 |
|---|---:|---:|---:|---:|
| Airline | 0.741 | 0.797 | 0.851 | 400 |
| Retail | 0.734 | 0.789 | 0.841 | 400 |
| Telecom | 0.752 | 0.810 | 0.857 | 400 |
| 合计 | 0.743 | 0.799 | 0.849 | 1200 |

---

# 附录 E：成本与 token 统计

## E.1 单条轨迹的 token 构成

在 Airline 域的完整 rollout 中，单条轨迹平均消耗 5,763.52 个总 token，其中：

- 智能体生成 4,068.86 个，占 70.6%；
- 用户模拟器生成 1,694.66 个，占 29.4%。

在 DIVERT 中，单条轨迹平均只消耗 3,272.96 个智能体 token，相比完整 rollout 减少 795.90 个，即减少 19.6%。同时，用户和框架侧 token 平均减少 563.56 个。

## E.2 共享前缀与 KV cache

本文没有直接测量 KV cache 的速度、延迟或内存收益，因为这些取决于服务系统、批处理策略、缓存管理方式和硬件。作者只测量精确共享前缀的比例，作为结构上是否存在缓存复用机会的代理指标。

| 领域 | 普通 rollout 的共享前缀 | DIVERT 分支的共享前缀 |
|---|---:|---:|
| Airline | 0.59% | 34.80% |
| Retail | 0.51% | 42.50% |
| Telecom | 0.54% | 58.35% |

这些数字不应被理解为实际 KV cache 加速，而只能说明 DIVERT 创建了适合缓存复用的精确共享前缀。

## E.3 DIVERT 的框架开销

每个 branch 的平均额外 token 成本为：

- junction 选择：103.48；
- 候选生成：3×108.68=326.04；
- 合计：429.52。

如果每条完整轨迹本来需要 4,068.86 个 agent token，而一个分支平均需要 3,272.96 个 agent token，则单个分支相对于等价完整 rollout 可以节约 agent token 795.90 个，另外节约评测侧 token 563.56 个。扣除 429.52 个框架开销后，论文计算的净 token 节约约为 929.94 个。

作者以 GPT-OSS-120B 作为廉价评测模型、以昂贵的前沿模型作为被测智能体，估计 DIVERT 框架开销占总体货币成本不到 1%，在代表性价格下约为 0.20% 或更低。

## E.4 τ-bench 成本估算

τ-bench 共包含 278 个任务：Airline 114 个、Retail 114 个、Telecom 50 个。按照每条轨迹平均 4,068.86 个 agent 输出 token，单次完整遍历约需要 1.13M 个 agent 输出 token。

如果每个任务进行 10 次 rollout，则约需要 11.31M 个 agent 输出 token。若采用 10 次 rollout 加每任务 12 个 branch，则约需 22.23M 个 agent 输出 token。按照论文中列出的代表性价格，仅计算输出 token 时，成本约为：Claude Opus 555.7 美元、GPT-5 222.3 美元、Gemini 3 Pro 266.8 美元。

这说明大规模、重复的前沿模型评测很快会变得昂贵，也解释了为什么作者把减少 agent token 作为主要优化目标。

---

# 附录 F：用户意图保持分析

在 τ-bench 的三个领域中，只有 Airline 提供了每个任务明确的 purpose 字段。因此作者使用 Airline 作为意图保持分析的代理领域。

对于每个分支点，作者把任务目的分别与原始用户消息和分支用户消息配对，并用 GPT-OSS-120B、temperature=0 的 LLM judge 判断消息是否仍然追求相同的核心目标。判断只关注意图方向，不关注措辞、质量或表达策略。

在 Airline 域的 700 个评测实例中：

- 原始模拟器消息有 207 条未能保持任务意图，占 28.12%；
- 分支消息有 186 条未能保持任务意图，占 25.27%。

因此，在这个自动判断和有限人工抽查的设置下，定向分支没有降低意图一致性，反而修正了一部分原始模拟器中的意图漂移。

---

# 附录 G：可复现性细节

## G.1 随机性和种子

所有实验使用固定随机种子 42。相同种子用于智能体 rollout、用户生成和分支逻辑，以保证重复实验的一致性。

## G.2 模型和解码配置

除特别说明外，智能体和用户模拟器使用相同模型骨干。解码设置为：

- 智能体 temperature：0.0；
- 用户 temperature：0.7；
- 每条轨迹最大模拟步数：100；
- 每条轨迹允许的最大错误数：10；
- 每次 LLM 调用最大重试次数：3。

## G.3 执行限制

轨迹在以下任一条件满足时终止：任务成功完成、达到 100 步上限，或错误数超过 10。

## G.4 代码

论文在此处说明代码可用性。

---

# 参考文献

参考文献作者、年份和论文题目按原文保留，便于检索：Agarwal et al. (2025), Bandel et al. (2026), Barres et al. (2025), Debenedetti et al. (2024), Erol et al. (2025), Kour et al. (2025), Kwon et al. (2023), Liu et al. (2023), Luo et al. (2025), Ma et al. (2026), Mialon et al. (2023), Nakash et al. (2025a, 2025b), Sekulić et al. (2024), Shim et al. (2025), Tang et al. (2026), Wang et al. (2025), Yao et al. (2023, 2024), Yehudai et al. (2025), Zhou et al. (2023a, 2023b)。

---

# 中英文术语对照表

下表优先采用本译稿中的中文表达；英文列保留论文、代码和工程讨论中常见的原词。

| 中文术语 | English | 本文语境中的含义 |
|---|---|---|
| 智能体 | agent | 能够进行多轮决策、调用工具并完成任务的 LLM 系统 |
| 大型语言模型 | large language model, LLM | 负责生成文本、决策或工具调用的模型 |
| 用户模拟器 | user simulator | 代替真实用户与智能体交互的模型或程序 |
| 智能体—用户交互 | agent–user interaction | 智能体与用户模拟器之间的完整交互过程 |
| 轨迹 | trajectory | 从任务开始到结束的一次完整执行记录 |
| 完整轨迹执行 | full rollout | 从初始状态开始，把整段对话重新执行一遍 |
| 线性 rollout | linear rollout | 每次从根节点独立生成一条直线式轨迹 |
| 蒙特卡洛 rollout | Monte Carlo rollout | 通过多次随机执行估计成功率或失败率 |
| 分支 | branch / branching | 从已有轨迹的中间状态出发，生成新的后续路径 |
| 轨迹分支 | trajectory branching | 在共享前缀之后探索多个可能的后续轨迹 |
| 快照 | snapshot | 对话、模型、工具和环境状态在某一时刻的完整保存 |
| 基于快照的恢复 | snapshot-based resumption | 从保存的中间状态恢复执行，而不是从头重跑 |
| 共享前缀 | shared prefix | 多条轨迹在分叉前完全相同的历史部分 |
| 根节点 | root state / root node | 任务最初的状态 |
| 交汇点 | junction | 可能显著改变后续智能体行为的关键交互位置 |
| 交汇点选择器 | junction chooser | 负责挑选最值得进行反事实分支的用户回合的模块 |
| 枢纽位置 | pivot point | 交汇点的另一种说法，强调它是行为分化的转折点 |
| 定向生成 | directed generation | 针对某一目标行为或失败模式生成用户回复 |
| 差异化用户回复 | divergent user response | 与原始回复不同，但仍试图完成同一任务的用户消息 |
| 多样性引导 | diversity-guided | 主动选择尚未探索、与已有路径不同的行为 |
| 语义多样性 | semantic diversity | 意义层面的差异，而不只是换几个词 |
| 意图保持 | intent preservation | 用户消息改变表达或策略，但仍追求原任务目标 |
| 意图漂移 | intent drift | 分支消息不再追求原任务目标 |
| 反事实 | counterfactual | 假设某一历史回合不同，观察后续会发生什么 |
| 反事实延续 | counterfactual continuation | 从同一历史状态替换一个输入后继续执行 |
| 覆盖率 | coverage | 已探索的任务、状态、行为或失败模式范围 |
| 任务级覆盖率 | task-level coverage | 至少发现一次失败的不同任务数量或比例 |
| 失败发现 | failure discovery | 找到智能体错误、违规或不可靠行为 |
| 失败模式 | failure mode | 一类可复现的错误机制或错误表现 |
| 失败根因 | failure root cause | 导致失败的更底层原因，例如验证绕过或状态错乱 |
| 错误发现率 | error discovery rate | 单位 token 成本下发现错误的数量 |
| 每 100K token 的错误数 | errors per 100K tokens | 每生成 100,000 个智能体 token 发现的失败数 |
| 任务失败数 | task failure count | 至少出现一次失败的不同任务数量 |
| 成功率 | success rate | 任务成功的运行次数占总运行次数的比例 |
| 一致性 | consistency | 同一任务多次运行时，智能体行为和结果的稳定程度 |
| 独立 rollout | independent rollout | 从同一根状态独立开始、彼此不共享中间状态的运行 |
| 分支 rollout | branched rollout | 从已有轨迹中间节点继续生成的运行 |
| 轨迹树 | trajectory tree | 由共享前缀和多个后续分支组成的交互结构 |
| 搜索空间 | search space | 智能体和用户可能产生的全部交互路径集合 |
| 迭代预算 | iteration budget | 允许进行多少轮分支和继续探索 |
| 分支预算 | branch budget | 允许额外生成多少条分支轨迹 |
| 候选回复 | candidate response | 用户模拟器为同一位置生成的备选消息 |
| 候选生成 | candidate generation | 生成多个备选用户消息的过程 |
| 多样性选择 | diversity selection | 从候选中挑选与已有回复或轨迹最不同的一个 |
| 余弦相似度 | cosine similarity | 用向量夹角衡量两个文本语义接近程度的指标 |
| 嵌入 | embedding | 将文本转换成向量表示的结果 |
| 共享嵌入空间 | shared embedding space | 不同文本可以在其中比较向量距离的表示空间 |
| 句子转换器 | sentence-transformers | 将句子编码为语义向量的模型框架 |
| 语义路径 | semantic path | 由一系列用户和智能体行为形成的意义层面的交互路线 |
| 罕见交互状态 | rare interaction state | 普通随机采样很难到达的状态 |
| 深层失败 | deep failure | 只有经过多轮交互或特定用户行为才会暴露的错误 |
| 工具调用 | tool call | 智能体请求外部 API、数据库或函数执行操作 |
| 工具输出 | tool output | 外部工具返回给智能体的结果 |
| 环境状态 | environment state | 数据库、工具、副作用和任务世界的当前状态 |
| 状态序列化 | state serialization | 把运行时状态保存成可恢复的文件或数据结构 |
| 精确回放 | exact replay | 在相同状态和条件下重新执行同一段轨迹 |
| 状态副作用 | state side effect | 工具调用对数据库或环境造成的持久变化 |
| KV 缓存 | KV cache | Transformer 推理时缓存历史 key/value 的机制 |
| 前缀复用 | prefix reuse | 对相同历史前缀避免重复计算 |
| token / 词元 | token | 模型输入或输出被切分后的基本文本单位 |
| token 成本 | token cost | 以模型输入输出 token 数衡量的计算或经济成本 |
| 评测开销 | evaluation overhead | 为进行评测额外消耗的模型调用、生成和管理成本 |
| 评测框架 | evaluation framework | 负责任务执行、用户模拟、状态管理和评分的系统 |
| 线性蒙特卡洛评测 | linear Monte Carlo evaluation | 通过大量独立完整运行估计统计表现的评测方式 |
| 覆盖率引导评测 | coverage-guided evaluation | 优先探索尚未覆盖的任务、状态或行为的评测方式 |
| LLM-as-a-judge | LLM 评审器 / 大模型裁判 | 使用另一个 LLM 判断轨迹是否成功或是否保持意图 |
| 后验判断 | post-hoc judgment | 运行完成后才进行的分析，不参与生成决策 |
| 任务目的 | task purpose | 任务希望用户最终实现的核心目标 |
| 用户背景 | user backstory | 用来约束用户身份、需求和可接受行为的背景信息 |
| 仁慈偏差 | benevolence bias | 用户模拟器倾向于过度合作的现象 |
| 非合作用户 | non-collaborative user | 会拒答、坚持、矛盾或制造摩擦的用户 |
| 对抗用户 | adversarial user | 有意测试智能体边界、策略或安全性的用户 |
| 红队评测 | red-teaming | 主动寻找安全漏洞、策略漏洞和异常行为的评测 |
| 提示注入 | prompt injection | 用户或外部内容试图改变智能体原始指令的攻击 |
| 仁慈用户 | benign user | 合作、善意且通常不主动制造困难的用户 |
| 领域 | domain | 一个服务场景，如航空、零售或电信 |
| 任务集合 | task set | 一个评测领域内的全部任务 |
| 成功谓词 | success predicate | 用程序或规则判断任务是否成功的条件 |
| 黄金标准 | gold success criterion | 任务预先定义的正确结果或可验证条件 |
| 消融实验 | ablation study | 逐项移除方法组件，观察各组件贡献的实验 |
| 异构设置 | heterogeneous setup | 智能体和用户模拟器使用不同模型的设置 |
| 温度 | temperature | 控制生成随机性和多样性的解码参数 |
| 随机种子 | random seed | 控制随机采样、便于复现实验的固定数值 |
| 终止条件 | termination criteria | 任务成功、步数超限或错误数超限等停止规则 |
| 交互时域 | interaction horizon | 一次任务允许经历的最长交互范围 |
| 成本—性能权衡 | cost–performance trade-off | 模型效果和调用成本之间的平衡 |
| 成本效益 | cost-effectiveness | 在给定成本下获得更多有效评测信息的能力 |
| 可复现性 | reproducibility | 其他人能够按照相同设置得到近似结果的程度 |
